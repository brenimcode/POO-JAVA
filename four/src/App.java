public class App {
    public static void main(String[] args) throws Exception {
        Caneta caca = new Caneta("PENTEL", "PRETA", 0.7f); // Recebe (modelo(str), cor(str), float(potna) )
        caca.status();
    }
}
